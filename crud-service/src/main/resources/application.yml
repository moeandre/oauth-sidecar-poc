server:
  port: 8081

spring:
  application:
    name: crud-service
  datasource:
    url: jdbc:h2:mem:tasksdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

logging:
  level:
    com.poc.crud: INFO
